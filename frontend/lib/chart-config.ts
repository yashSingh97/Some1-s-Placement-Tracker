// Chart animation configuration
// Adjust ANIMATION_DURATION to control how fast animations play (in milliseconds)
export const CHART_CONFIG = {
  ANIMATION_DURATION: 100, // ms - reduce for faster, increase for slower animations
} as const
